streamlit
yfinance

# ML model dependencies (required to train and run the XGBoost model)
xgboost
scikit-learn
joblib
pyarrow
matplotlib

# WRDS data extraction (only needed to run 01_pull_wrds_data.py)
# wrds
